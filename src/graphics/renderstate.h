#ifndef __RENDER_STATE_H__
#define __RENDER_STATE_H__

// World units per metre (kept here; many files include it for this).
#define SCENE_SCALE             128

// struct RenderState is per machine: the display list and its memory on the
// N64, the GE's vertex scratch on the PSP.
#include "render_state.h"

#endif
